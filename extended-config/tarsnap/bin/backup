# Modified from "caf's" post here:
# https://news.ycombinator.com/item?id=7514380

# Requires a valid `keyfile` to be set in .tarsnaprc

TARGETS="/home/traveller"
ARCHIVE="$(hostname)_$(date +%Y-%m-%d_%H-%M)"

# -c    Create archive.
# -f    Specify archive name.
tarsnap -c -f "$ARCHIVE" $TARGETS
